int [] arrMass = { 1, 9, 9, 0, 2, 8, 0, 9 };
int length = arrMass.Length;

int index = 0;
while(index <= length)
{
if(index[index]=arrMass[0])
{
    //Console.WriteLine("1 " + index[index]);
}
    index++;
}